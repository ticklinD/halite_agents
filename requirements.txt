# Halite — hybrid TUI coding agent
# Core runtime dependencies (mirrors [project].dependencies in pyproject.toml).
# Install with one of:
#   python3 -m pip install -r requirements.txt
#   uv pip install --python .venv/bin/python -r requirements.txt

# --- UI / foundation ---
textual>=0.40.0
rich>=13.0
pydantic>=2.0
loguru>=0.7
tomli-w>=1.0

# --- Secrets ---
keyring>=25.0
cryptography>=41.0

# --- Inference backends ---
ollama>=0.1.0
anthropic>=0.30

# --- Tools (feature / capstone workflows) ---
# Playwright is only imported lazily by the browser tool; needs `playwright install`
# (and its browser binaries) before the BrowserTool is usable.
playwright>=1.40
reportlab>=4.0

# --- Dev / test ---
pytest>=8.0
pytest-asyncio>=0.23
